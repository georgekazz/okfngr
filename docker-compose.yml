version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: okfn-app
    restart: always
    ports:
      - "${APP_PORT:-8059}:80"
    environment:
      APP_NAME: "OKFN Greece"
      APP_ENV: production
      APP_DEBUG: "false"
      APP_URL: ${APP_URL:-http://localhost:8059}
      APP_KEY: ${APP_KEY}
      DB_CONNECTION: mysql
      DB_HOST: db
      DB_PORT: 3306
      DB_DATABASE: ${DB_DATABASE:-okfngr}
      DB_USERNAME: ${DB_USERNAME:-okfnuser}
      DB_PASSWORD: ${DB_PASSWORD:-okfnpass}
    volumes:
      - storage_data:/var/www/html/storage/app/public
    depends_on:
      db:
        condition: service_healthy   # ← wait for DB to be truly ready

  db:
    image: mysql:8.0
    container_name: okfn-db
    restart: always
    environment:
      MYSQL_DATABASE: ${DB_DATABASE:-okfngr}
      MYSQL_USER: ${DB_USERNAME:-okfnuser}
      MYSQL_PASSWORD: ${DB_PASSWORD:-okfnpass}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-okfnroot}
    ports:
      - "${DB_PORT:-3339}:3306"
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_ROOT_PASSWORD:-okfnroot}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

volumes:
  db_data:
  storage_data: